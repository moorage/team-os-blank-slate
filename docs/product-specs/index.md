# Product specs index

This directory holds user-visible requirements and acceptance criteria.

Status by spec:

- `team-os-text-kanban.md` — implemented first slice for text-native workflow tracking, product artifact linking, and native write-gating contracts
- `team-os-native-apps.md` — implemented second slice for buildable Apple shells, repo-access readiness UI, and live GitHub branch/PR services

Maintenance rule:
- when active user-visible behavior changes, update the matching spec in the same change
- when a planned area becomes implemented, change its status and acceptance to match the code
